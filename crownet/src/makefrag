#
# Use the new message compiler introduced in OMNeT++ 5.3
#
MSGC:=$(MSGC) --msg6

#
# compiler warning for c-style cast
#
#CFLAGS += -Wold-style-cast
#CFLAGS += -save-temps
CXXFLAGS += -Wno-format-security

# use the following options to debug memory allocation problems 
# (disabled by default since it leads to long runtimes)
#COPTS += -O1 -g -fsanitize=address,undefined -fno-omit-frame-pointer -Wno-format-security
#COPTS += -O1 -g -fsanitize=address -fno-omit-frame-pointer
#LIBS += -lubsan