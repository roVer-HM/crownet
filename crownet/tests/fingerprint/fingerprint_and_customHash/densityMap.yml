# Fingerprint tests for the "densityMap" scenario.
# See crownet_fingerprinttest crownet --help
#   * workingdir is relative to the given '-C' flag of crownet_fingerprinttest
#   * each toplevel key will be one test case
#   * if first item in `args` does not start with an `-` it will be
#     interpreted as the command to execute
#   * if fingerprint starts with 'file:' read the given path. If path
#     is relative it based on workingdir. An empty path will default
#     to hash.d/<testcase-name>.csv relative to the current files location

vadere_circle:
  wd: simulations/densityMap
  args:
    - run_script.py
    - vadere-opp
    - --create-vadere-container
    - --override-host-config
    - --opp.-c
    - vadere_circle
    - --opp.--sim-time-limit=40s
  simtimelimit: 40s
  expectedResult: PASS
  fingerprint: "file:"
  tags:
    exec: python3
    type: shell

vadere_1d_const:
  wd: simulations/densityMap
  args:
    - run_script.py
    - vadere-opp
    - --create-vadere-container
    - --override-host-config
    - --opp.-c
    - vadere_1d_const
    - --opp.--sim-time-limit=40s
  simtimelimit: 40s
  expectedResult: PASS
  fingerprint: "file:"
  tags:
    exec: python3
    type: shell

S0_bonn_motion_entropy_dev:
  wd: simulations/densityMap
  args:
    - run_script.py
    - omnet
    - --override-host-config
    - --opp.-c
    - S0_bonn_motion_entropy_dev
    - --opp.--sim-time-limit=100s
  simtimelimit: 100s
  expectedResult: PASS
  fingerprint: "file:"
  tags:
    exec: python3
    type: shell