<config>
    <interface hosts='*' address='10.x.x.x' netmask='255.255.255.0'/>
    <!-- <route hosts="*"> -->
    <!-- all UEs participate in the multicast group --> 
    <multicast-group hosts="ue[*]" interfaces="wlan" address="224.0.0.10"/>
</config>