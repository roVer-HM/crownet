# setup of crownet environment (aliases and path)
#
# Note: Include this script in the startup file of your shell,
#       e.g. by adding "source [path to this script]/crownetenv" at the end of ~/.bashrc
SCRIPT_DIR=$( cd -- "$( dirname -- "${BASH_SOURCE[0]}" )" &> /dev/null && pwd )

# Configure repository settings: 
# uncomment the following line to use settings for public crownet repository
source $SCRIPT_DIR/crownetenv_pub
# uncomment the following line to use settings for private crownet repository (hosted at MUAS)
# source $SCRIPT_DIR/crownetenv_dev

